name: Deploy Website

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy my website
        run: /srv/scripts/deploy/my-website-deploy.sh
